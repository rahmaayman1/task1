#include<iostream>
using namespace std;

int main() {
	int max, num;
	cin >> max;
	cin >> num; if (max < num) max = num;
	cin >> num; if (max < num) max = num;
	cin >> num; if (max < num) max = num;
	cin >> num; if (max < num) max = num;
	cin >> num; if (max < num) max = num;
	cin >> num; if (max < num) max = num;
	cin >> num; if (max < num) max = num;
	cin >> num; if (max < num) max = num;
	cin >> num; if (max < num) max = num;
	cout << max;

}

